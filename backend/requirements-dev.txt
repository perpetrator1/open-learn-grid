# Development-only dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
pytest-xdist>=3.5        # parallel test execution with -n auto
pytest-asyncio>=0.23

# Test factories / fixtures
factory-boy>=3.3
faker>=24.0

# API testing
httpx>=0.27             # async-compatible test client

# Linting / formatting
ruff>=0.4

# Type checking
mypy>=1.10
django-stubs[compatible-mypy]>=5.0
djangorestframework-stubs>=3.15

# Debug toolbar (useful in development)
django-debug-toolbar>=4.3

# Shell enhancements
ipython>=8.0
django-extensions>=3.2
